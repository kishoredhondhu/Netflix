BudgetManagementComponent,
BudgetFormComponent,
BudgetDetailComponent,
TeamManagementComponent,
TeamMemberFormComponent,


],
imports: [
BrowserModule,
ReactiveFormsModule, // For model-driven forms
FormsModule,         // For template-driven forms (if any component uses them)
HttpClientModule,    // For HTTP requests
AppRoutingModule     // For application routing (ensure this file exists and is configured)
],
providers: [
// Services with @Injectable({ providedIn: 'root' }) don't need to be listed here.
// e.g., ProjectService, BudgetService, TeamMemberService are likely root-provided.
// If CallSheetService is not root-provided, add it here:
// CallSheetService
],
bootstrap: [AppComponent]
})
export class AppModule { }
